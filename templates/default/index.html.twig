{% extends 'base.html.twig' %}

{% block header_title %}
    <h1>{{ 'page.index.title'|trans }}</h1>
{% endblock %}

{% block body %}

    <div class="row">
        <a href="{{ path('task_create') }}" class="btn btn-success">{{ 'page.index.button.add.task'|trans }}</a>
        <a href="{{ path('task_list') }}" class="btn btn-info">{{ 'page.index.button.list.task.open'|trans }}</a>
        <a href="" class="btn btn-secondary">{{ 'page.index.button.list.task.close'|trans }}</a>
    </div>
{% endblock %}
