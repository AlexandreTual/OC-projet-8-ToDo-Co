language: php
php:
  - '7.2.0'

services:
  - mysql

env:
  global:
    - SYMFONY_VERSION="4.3.*" DB=mysql

install:
  - composer install
  - cp .env.travis .env
  - bin/console doctrine:database:create
  - bin/console doctrine:schema:update

script:
  - bin/phpunit